document.addEventListener('DOMContentLoaded', () => {
  const style = document.createElement('style');
  style.textContent = `
#schoolYearNav{margin-top:8px;background:#203252;border:1px solid #3d5277;color:#fff}#schoolYearNav:hover,#schoolYearNav:focus{background:#38527e}
#schoolYearModal{padding:0;background:#0c1425b8;place-items:stretch}#schoolYearModal .year-shell{width:100%;height:100vh;max-height:none;border-radius:0;overflow:hidden;display:flex;flex-direction:column;background:#f5f7fb}
.year-topbar{flex:none;background:#fff;border-bottom:1px solid var(--line);padding:18px clamp(18px,4vw,56px);display:flex;align-items:center;justify-content:space-between;gap:24px}.year-title-wrap{display:flex;align-items:center;gap:14px}.year-mark{width:42px;height:42px;border-radius:12px;background:#e9efff;color:var(--blue);display:grid;place-items:center;font-size:21px}.year-topbar h2{font-size:22px;margin:0}.year-close{width:42px;height:42px;border-radius:50%;background:#eef1f6;color:#475467;font-size:25px}
.year-workspace{flex:1;min-height:0;display:grid;grid-template-columns:minmax(230px,280px) minmax(0,1fr);overflow:hidden}.year-sidebar{background:#fff;border-right:1px solid var(--line);padding:28px 22px;overflow:auto}.year-steps{display:grid;gap:8px}.year-step{display:flex;gap:12px;padding:13px;border-radius:11px;color:#667085}.year-step.active{background:#edf2ff;color:#254fbb}.year-step.done{color:#08784b}.year-step-num{width:27px;height:27px;flex:none;border:1px solid currentColor;border-radius:50%;display:grid;place-items:center;font-weight:850;font-size:12px}.year-step strong{display:block;font-size:14px}.year-step small{display:block;margin-top:3px;line-height:1.35}.year-safety{margin-top:28px;padding:15px;border-radius:11px;background:#f0f9f4;color:#176b4a;font-size:13px;line-height:1.5}.year-safety b{display:block;margin-bottom:4px}
.year-main{overflow:auto;padding:clamp(22px,4vw,48px);scroll-behavior:smooth}.year-content{width:min(1160px,100%);margin:0 auto}.year-section-head{display:flex;justify-content:space-between;gap:20px;align-items:flex-start;margin-bottom:22px}.year-section-head h3{font-size:25px;margin:0 0 6px}.year-section-head p{margin:0;color:var(--muted);line-height:1.5}.year-year-card{display:grid;grid-template-columns:minmax(220px,330px) 1fr;gap:22px;background:#fff;border:1px solid var(--line);border-radius:16px;padding:24px;box-shadow:0 4px 16px #10182808}.year-year-card input{font-size:24px;font-weight:850;min-height:58px}.year-year-help{align-self:center;color:#475467;line-height:1.55}.year-year-help b{display:block;color:var(--navy);margin-bottom:4px}
.year-mode-grid{display:grid;grid-template-columns:1fr 1fr;gap:14px;margin-top:18px}.year-mode{border:2px solid #d9e1ef;border-radius:16px;padding:20px;background:#fff;text-align:left;color:#344054}.year-mode.selected{border-color:#2563eb;background:#eff6ff;color:#163c8c;box-shadow:0 8px 24px #2563eb18}.year-mode strong{display:block;font-size:17px;margin-bottom:6px}.year-mode span{display:block;line-height:1.45;font-size:13px}.year-mode-icon{width:38px;height:38px;border-radius:11px;background:#dbeafe;color:#1d4ed8;display:grid;place-items:center;margin-bottom:13px;font-size:19px}
.year-toolbar{display:flex;gap:10px;align-items:center;flex-wrap:wrap;margin-bottom:15px}.year-toolbar .search{flex:1;min-width:220px}.year-progress-text{margin-left:auto;font-size:13px;font-weight:750;color:#475467}.year-class-list{display:grid;gap:14px}.year-class-card{background:#fff;border:1px solid var(--line);border-radius:15px;overflow:hidden;box-shadow:0 3px 12px #10182808}.year-class-card.complete{border-color:#a6dfc5}.year-class-head{padding:18px 20px;display:grid;grid-template-columns:minmax(140px,1fr) 26px minmax(150px,1fr) minmax(135px,180px) auto;gap:12px;align-items:center}.year-class-name strong{display:block;font-size:17px}.year-count{font-size:12px;color:var(--muted);margin-top:4px}.year-arrow{text-align:center;color:#98a2b3;font-size:20px}.year-class-head input,.year-class-head select{min-height:42px}.year-class-actions{display:flex;gap:7px}.year-class-actions .btn{white-space:nowrap;min-height:42px}.year-status-button{background:#f8faff;border:1px solid #d8e2ff;color:#315dbb}.year-students{border-top:1px solid var(--line);background:#fbfcfe;padding:8px 20px 16px}.year-student{display:grid;grid-template-columns:minmax(180px,1.4fr) minmax(150px,.8fr) minmax(150px,1fr) minmax(125px,.7fr);gap:10px;align-items:center;padding:10px 0;border-bottom:1px solid #edf0f4}.year-student:last-child{border-bottom:0}.year-student-name{font-weight:700}.year-student select,.year-student input{min-height:40px;padding:8px 10px}.year-empty-filter{text-align:center;padding:45px;color:var(--muted);background:#fff;border:1px solid var(--line);border-radius:14px}
.year-review-grid{display:grid;grid-template-columns:repeat(4,1fr);gap:12px;margin-bottom:18px}.year-review-stat{background:#fff;border:1px solid var(--line);border-radius:14px;padding:18px}.year-review-stat span{font-size:12px;color:var(--muted);font-weight:700}.year-review-stat strong{display:block;font-size:27px;margin-top:6px}.year-review-box{background:#fff;border:1px solid var(--line);border-radius:15px;padding:22px;line-height:1.55}.year-review-box.warning{border-color:#f2c58a;background:#fffaf3}.year-review-box.success{border-color:#a6dfc5;background:#f5fcf8}.year-review-box h4{margin:0 0 10px;font-size:17px}.year-review-list{display:grid;gap:8px;margin-top:14px}.year-review-row{display:flex;justify-content:space-between;gap:20px;padding-top:8px;border-top:1px solid #e8ecef}
.year-footer{flex:none;background:#fff;border-top:1px solid var(--line);padding:14px clamp(18px,4vw,56px);display:flex;justify-content:space-between;align-items:center;gap:14px}.year-footer-status{font-size:13px;color:var(--muted)}.year-footer-status b{display:block;color:var(--navy);font-size:14px;margin-bottom:2px}.year-footer-actions{display:flex;gap:10px}.year-footer .btn{min-width:140px}.year-spinner{width:17px;height:17px;border:2px solid #ffffff70;border-top-color:#fff;border-radius:50%;animation:year-spin .7s linear infinite}@keyframes year-spin{to{transform:rotate(360deg)}}.year-error{margin-top:14px;padding:13px;border-radius:10px;background:#fff0ed;color:var(--danger);font-size:13px}
.year-success{min-height:60vh;display:grid;place-items:center;text-align:center}.year-success-card{width:min(560px,100%);background:#fff;border:1px solid #a6dfc5;border-radius:20px;padding:42px;box-shadow:0 14px 40px #10182812}.year-success-icon{width:72px;height:72px;margin:0 auto 18px;border-radius:50%;display:grid;place-items:center;background:#e7f8ef;color:#08784b;font-size:34px}.year-success h3{font-size:27px;margin:0 0 8px}.year-success p{color:var(--muted);line-height:1.55}
.year-builder-new{display:grid;grid-template-columns:1fr 160px auto;gap:10px;background:#fff;border:1px solid var(--line);border-radius:15px;padding:16px;margin-bottom:16px}.year-builder-actions{display:flex;gap:8px;flex-wrap:wrap;background:#edf4ff;border:1px solid #c9dafc;border-radius:14px;padding:13px;margin-bottom:16px}.year-builder-actions select{min-width:190px}.year-builder-grid{display:grid;grid-template-columns:minmax(330px,1.1fr) minmax(280px,.9fr);gap:16px}.year-pool,.year-destinations{display:grid;gap:10px;align-content:start}.year-pool-card,.year-destination-card{background:#fff;border:1px solid var(--line);border-radius:13px;padding:14px}.year-pool-card{display:flex;gap:11px;align-items:center}.year-pool-card input{width:18px;height:18px}.year-pool-card.terminal{border-color:#eab676;background:#fffaf2}.year-pool-card b,.year-destination-card b{display:block}.year-pool-card small,.year-destination-card small{color:var(--muted)}.year-terminal-badge{display:inline-block;margin-top:5px;padding:3px 7px;border-radius:999px;background:#fff0d5;color:#9a5b00;font-size:11px;font-weight:750}.year-destination-card{border-left:4px solid #2563eb}.year-destination-card button{float:right;color:#b42318;background:transparent}.year-review-grid.five{grid-template-columns:repeat(5,1fr)}
@media(max-width:980px){.year-workspace{grid-template-columns:1fr}.year-sidebar{display:none}.year-class-head{grid-template-columns:1fr 22px 1fr}.year-class-actions{grid-column:1/-1}.year-class-actions .btn{flex:1}.year-student{grid-template-columns:1fr 1fr}.year-review-grid,.year-review-grid.five{grid-template-columns:1fr 1fr}.year-builder-grid{grid-template-columns:1fr}}@media(max-width:620px){.year-topbar{padding:13px 15px}.year-title-wrap .meta{display:none}.year-main{padding:18px 14px}.year-year-card,.year-mode-grid,.year-builder-new{grid-template-columns:1fr}.year-year-card{padding:18px}.year-toolbar .search{min-width:100%}.year-progress-text{margin-left:0}.year-class-head,.year-student{grid-template-columns:1fr}.year-arrow{transform:rotate(90deg)}.year-class-actions{display:grid}.year-students{padding:6px 14px 14px}.year-footer{padding:11px 14px}.year-footer-status{display:none}.year-footer-actions{width:100%}.year-footer .btn{min-width:0;flex:1}.year-success-card{padding:28px 20px}}
  `;
  document.head.appendChild(style);

  const button = document.createElement('button');
  button.id = 'schoolYearNav'; button.type = 'button'; button.className = 'hidden';
  button.innerHTML = '↗ &nbsp; Preparar novo ano letivo';
  document.querySelector('.side .nav')?.appendChild(button);

  const modal = document.createElement('div');
  modal.id = 'schoolYearModal'; modal.className = 'modal-bg hidden';
  modal.innerHTML = `<section class="year-shell" role="dialog" aria-modal="true" aria-labelledby="schoolYearTitle"><header class="year-topbar"><div class="year-title-wrap"><div class="year-mark">↗</div><div><h2 id="schoolYearTitle">Preparar novo ano letivo</h2><div class="meta">Organize a passagem das turmas antes de confirmar.</div></div></div><button class="year-close" type="button" data-year-close aria-label="Fechar">×</button></header><div class="year-workspace"><aside class="year-sidebar"><div class="year-steps"><div class="year-step active" data-step-indicator="1"><span class="year-step-num">1</span><div><strong>Novo ano</strong><small>Escolha o ano letivo de destino.</small></div></div><div class="year-step" data-step-indicator="2"><span class="year-step-num">2</span><div><strong>Turmas e alunos</strong><small>Defina destinos e ajuste exceções.</small></div></div><div class="year-step" data-step-indicator="3"><span class="year-step-num">3</span><div><strong>Revisar</strong><small>Confira tudo antes de concluir.</small></div></div></div><div class="year-safety"><b>Seu histórico fica protegido</b>Alunos, ocorrências e registros anteriores são preservados. As turmas atuais serão arquivadas, não excluídas.</div></aside><main class="year-main"><div class="year-content" id="schoolYearContent"></div></main></div><footer class="year-footer"><div class="year-footer-status" id="schoolYearFooterStatus"></div><div class="year-footer-actions"><button id="schoolYearBack" class="btn secondary" type="button">Voltar</button><button id="schoolYearNext" class="btn primary" type="button">Continuar</button></div></footer></section>`;
  document.body.appendChild(modal);

  const content = modal.querySelector('#schoolYearContent'), back = modal.querySelector('#schoolYearBack'), next = modal.querySelector('#schoolYearNext'), footer = modal.querySelector('#schoolYearFooterStatus');
  const normalize = value => String(value || '').normalize('NFD').replace(/[\u0300-\u036f]/g, '').trim().toLowerCase();
  const canPrepare = () => permission?.role === 'admin' || (permission?.is_coordinator && permission?.can_prepare_school_year);
  let step = 1, busy = false, targetYear, assignments, expanded, filter, mode, destinations, selected;

  const isTerminalClass = name => /(^|\s)3\s*(ª|a)\s*s[eé]rie(\s|$)/i.test(String(name || '')) || /(^|\s)3\s*(º|o)?\s*ano\s*(do\s*)?(ensino\s*m[eé]dio|em)(\s|$)/i.test(String(name || ''));

  function suggestNextClass(name) {
    const match = String(name || '').match(/^(.*?)(\d{1,2})(\s*[º°ª]?\s*.*)$/u);
    return match && Number(match[2]) < 99 ? `${match[1]}${Number(match[2]) + 1}${match[3]}`.trim() : '';
  }
  function resetPlan() {
    targetYear = new Date().getFullYear() + 1; step = 1; busy = false; expanded = new Set(); filter = ''; mode = 'transfer'; destinations = []; selected = new Set();
    assignments = new Map(students.map(student => [student.id, { student_id:student.id, status:'pending', class_name:'', shift:classes.find(item => item.id === student.classId)?.shift || 'Matutino' }]));
  }
  function stats() {
    const values = [...assignments.values()];
    return { pending:values.filter(x => x.status === 'pending').length, moved:values.filter(x => x.status === 'active').length, repeated:values.filter(x => x.status === 'repeated').length, transferred:values.filter(x => x.status === 'transferred').length, concluded:values.filter(x => x.status === 'concluded').length, classes:new Set(values.filter(x => ['active','repeated'].includes(x.status)).map(x => `${normalize(x.class_name)}|${x.shift}`)).size };
  }
  function setAssignment(id, changes) { assignments.set(id, { ...assignments.get(id), ...changes }); }
  function applyClass(classId, destination, shift) { const current=classes.find(c=>c.id===classId);if(isTerminalClass(current?.name)){toast('A 3ª série é uma etapa terminal. Defina cada aluno como concluinte, repetente ou transferido.');return false}students.filter(s => s.classId === classId).forEach(s => setAssignment(s.id, { status:'active', class_name:destination, shift }));return true }
  function classComplete(classId) { const list = students.filter(s => s.classId === classId); return list.length && list.every(s => { const x=assignments.get(s.id); return x?.status !== 'pending' && (x.status === 'transferred' || x.class_name); }); }
  function validate() { const values=[...assignments.values()], pending=values.filter(x=>x.status==='pending'), missing=values.filter(x=>['active','repeated'].includes(x.status)&&!x.class_name), invalidTerminal=students.filter(s=>isTerminalClass(s.className)&&assignments.get(s.id)?.status==='active'); return pending.length ? `${pending.length} aluno${pending.length===1?'':'s'} com situação ainda não definida` : missing.length ? `${missing.length} aluno${missing.length===1?'':'s'} sem turma de destino` : invalidTerminal.length ? 'Alunos aprovados da 3ª série devem ser marcados como concluintes.' : ''; }
  function updateChrome() {
    modal.querySelectorAll('[data-step-indicator]').forEach(item => { const n=Number(item.dataset.stepIndicator); item.classList.toggle('active',n===step); item.classList.toggle('done',n<step); item.querySelector('.year-step-num').textContent=n<step?'✓':n; });
    back.classList.toggle('hidden',step===1||busy); next.disabled=busy; next.innerHTML=busy?'<span class="year-spinner"></span> Preparando…':step===3?'Confirmar e preparar':'Continuar';
    const s=stats(); footer.innerHTML=step===1?`<b>${students.length} alunos em ${classes.length} turmas</b>Nenhuma alteração será feita até a confirmação final.`:step===2?`<b>${students.length-s.pending} de ${students.length} alunos definidos</b>${s.pending?`${s.pending} ainda precisam de uma situação.`:'Plano pronto para revisão.'}`:`<b>Revisão do ano ${targetYear}</b>Confira os destinos antes de confirmar.`;
  }
  function renderYear() {
    content.innerHTML=`<div class="year-section-head"><div><h3>Como você quer preparar ${targetYear}?</h3><p>Escolha a forma mais simples para a realidade desta escola.</p></div></div><div class="year-year-card"><div><label for="schoolYearTarget">Ano letivo de destino</label><input id="schoolYearTarget" type="number" min="2020" max="2200" value="${targetYear}"></div><div class="year-year-help"><b>Você ainda está apenas planejando.</b>Nada muda até a revisão e a confirmação final. Alunos, fotos, ocorrências e frequências permanecem ligados ao mesmo perfil.</div></div><div class="year-mode-grid"><button class="year-mode ${mode==='transfer'?'selected':''}" data-year-mode="transfer" type="button"><span class="year-mode-icon">→</span><strong>Transferir por turma</strong><span>Use a turma atual como ponto de partida e ajuste somente as exceções.</span></button><button class="year-mode ${mode==='build'?'selected':''}" data-year-mode="build" type="button"><span class="year-mode-icon">▦</span><strong>Montar turmas do próximo ano</strong><span>Crie as turmas de ${targetYear} e distribua livremente alunos de turmas misturadas.</span></button></div>`;
    content.querySelector('input').oninput=e=>{targetYear=Number(e.target.value)};
    content.querySelectorAll('[data-year-mode]').forEach(item=>item.onclick=()=>{mode=item.dataset.yearMode;renderYear()});
  }
  function studentRows(classId) {
    return students.filter(s=>s.classId===classId).map(s=>{const x=assignments.get(s.id), terminal=isTerminalClass(s.className), enabled=['active','repeated'].includes(x.status);return `<div class="year-student" data-student-id="${esc(s.id)}"><div><div class="year-student-name">${esc(s.name)}</div><div class="meta">${terminal?'3ª série · etapa terminal':'Ajuste individual'}</div></div><select data-field="status"><option value="pending" ${x.status==='pending'?'selected':''}>Situação ainda não definida</option>${terminal?'':`<option value="active" ${x.status==='active'?'selected':''}>Remanejar</option>`}<option value="repeated" ${x.status==='repeated'?'selected':''}>Repetente</option><option value="transferred" ${x.status==='transferred'?'selected':''}>Transferido da escola</option>${terminal?`<option value="concluded" ${x.status==='concluded'?'selected':''}>Concluiu a etapa</option>`:''}</select><input data-field="class_name" value="${esc(x.class_name)}" placeholder="Turma de destino" ${enabled?'':'disabled'}><select data-field="shift" ${enabled?'':'disabled'}><option ${x.shift==='Matutino'?'selected':''}>Matutino</option><option ${x.shift==='Vespertino'?'selected':''}>Vespertino</option><option ${x.shift==='Noturno'?'selected':''}>Noturno</option></select></div>`}).join('');
  }
  function renderClasses() {
    const visible=classes.filter(c=>!filter||normalize(c.name).includes(normalize(filter))||students.some(s=>s.classId===c.id&&normalize(s.name).includes(normalize(filter)))), s=stats();
    content.innerHTML=`<div class="year-section-head"><div><h3>Para onde cada turma vai?</h3><p>Use a sugestão automática e abra “Ajustar alunos” somente quando houver exceções.</p></div></div><div class="year-toolbar"><input id="schoolYearFilter" class="search" value="${esc(filter)}" placeholder="Buscar turma ou aluno"><button id="schoolYearAuto" class="btn secondary" type="button">Sugerir próximas turmas</button><span class="year-progress-text">${students.length-s.pending}/${students.length} definidos</span></div><div class="year-class-list">${visible.length?visible.map(c=>{const list=students.filter(s=>s.classId===c.id), first=assignments.get(list[0]?.id), shared=list.length&&list.every(s=>assignments.get(s.id)?.class_name===first?.class_name)?first?.class_name||'':'', shift=list.length&&list.every(s=>assignments.get(s.id)?.shift===first?.shift)?first?.shift||c.shift||'Matutino':c.shift||'Matutino', open=expanded.has(c.id)||(filter&&list.some(s=>normalize(s.name).includes(normalize(filter))));return `<article class="year-class-card ${classComplete(c.id)?'complete':''}" data-class-id="${esc(c.id)}"><div class="year-class-head"><div class="year-class-name"><strong>${esc(c.name)}</strong><div class="year-count">${list.length} aluno${list.length===1?'':'s'} ${classComplete(c.id)?'· definida ✓':''}</div></div><div class="year-arrow">→</div><input data-class-destination value="${esc(shared)}" placeholder="Ex.: 7º E"><select data-class-shift><option ${shift==='Matutino'?'selected':''}>Matutino</option><option ${shift==='Vespertino'?'selected':''}>Vespertino</option><option ${shift==='Noturno'?'selected':''}>Noturno</option></select><div class="year-class-actions"><button class="btn primary" type="button" data-apply-class>Aplicar à turma</button><button class="btn year-status-button" type="button" data-toggle-students>${open?'Ocultar alunos':'Ajustar alunos'}</button></div></div>${open?`<div class="year-students">${studentRows(c.id)}</div>`:''}</article>`}).join(''):'<div class="year-empty-filter">Nenhuma turma ou aluno encontrado.</div>'}</div>`;
    content.querySelector('#schoolYearFilter').oninput=e=>{filter=e.target.value;renderClasses();content.querySelector('#schoolYearFilter')?.focus()};
    content.querySelector('#schoolYearAuto').onclick=()=>{classes.filter(c=>!isTerminalClass(c.name)).forEach(c=>{const d=suggestNextClass(c.name);if(d)applyClass(c.id,d,c.shift||'Matutino')});renderClasses();toast('Sugestões aplicadas. A 3ª série ficou pendente para conclusão, repetência ou transferência.')};
    content.querySelectorAll('[data-class-id]').forEach(card=>{const id=card.dataset.classId;card.querySelector('[data-apply-class]').onclick=()=>{const d=card.querySelector('[data-class-destination]').value.trim();if(!d){toast('Informe a turma de destino.');return}if(applyClass(id,d,card.querySelector('[data-class-shift]').value)){renderClasses();toast(`Destino aplicado a ${classes.find(c=>c.id===id)?.name||'turma'}.`)}};card.querySelector('[data-toggle-students]').onclick=()=>{expanded.has(id)?expanded.delete(id):expanded.add(id);renderClasses()}});
    content.querySelectorAll('[data-student-id]').forEach(row=>row.querySelectorAll('[data-field]').forEach(field=>field.onchange=()=>{const status=row.querySelector('[data-field="status"]').value;setAssignment(row.dataset.studentId,{status,class_name:['pending','transferred','concluded'].includes(status)?'':row.querySelector('[data-field="class_name"]').value.trim(),shift:row.querySelector('[data-field="shift"]').value});renderClasses()}));
    updateChrome();
  }
  function renderBuilder(){
    const s=stats(),pendingStudents=students.filter(student=>assignments.get(student.id)?.status==='pending');
    content.innerHTML=`<div class="year-section-head"><div><h3>Monte as turmas de ${targetYear}</h3><p>Crie as turmas de destino, selecione alunos e mova vários de uma vez.</p></div><button class="btn secondary" id="yearResetBuilder" type="button">Recomeçar distribuição</button></div><div class="year-builder-new"><input id="yearNewClassName" placeholder="Nome da turma, ex.: 9º A"><select id="yearNewClassShift"><option>Matutino</option><option>Vespertino</option><option>Noturno</option></select><button class="btn primary" id="yearAddDestination" type="button">Adicionar turma</button></div><div class="year-builder-actions"><select id="yearBatchDestination"><option value="">Escolha a turma de destino</option>${destinations.map((d,i)=>`<option value="${i}">${esc(d.name)} · ${esc(d.shift)}</option>`).join('')}</select><button class="btn primary" data-batch="active" type="button">Mover selecionados</button><button class="btn secondary" data-batch="repeated" type="button">Marcar repetentes</button><button class="btn secondary" data-batch="transferred" type="button">Transferidos</button><button class="btn secondary" data-batch="concluded" type="button">Concluíram a 3ª série</button></div><div class="year-builder-grid"><section><h4>Alunos sem destino (${pendingStudents.length})</h4><div class="year-pool">${pendingStudents.length?pendingStudents.map(student=>{const terminal=isTerminalClass(student.className);return `<label class="year-pool-card ${terminal?'terminal':''}"><input type="checkbox" data-pool-student="${esc(student.id)}" ${selected.has(student.id)?'checked':''}><span><b>${esc(student.name)}</b><small>${esc(student.className||'Sem turma')}</small>${terminal?'<span class="year-terminal-badge">3ª série · não pode ser remanejado</span>':''}</span></label>`}).join(''):'<div class="year-review-box success">Todos os alunos possuem uma situação definida.</div>'}</div></section><section><h4>Turmas de ${targetYear} (${destinations.length})</h4><div class="year-destinations">${destinations.length?destinations.map((d,i)=>{const count=[...assignments.values()].filter(x=>['active','repeated'].includes(x.status)&&normalize(x.class_name)===normalize(d.name)&&x.shift===d.shift).length;return `<div class="year-destination-card"><button type="button" data-remove-destination="${i}" aria-label="Remover turma">×</button><b>${esc(d.name)}</b><small>${esc(d.shift)} · ${count} aluno${count===1?'':'s'}</small></div>`}).join(''):'<div class="year-review-box">Adicione as turmas que existirão no próximo ano.</div>'}</div></section></div>`;
    content.querySelector('#yearAddDestination').onclick=()=>{const name=content.querySelector('#yearNewClassName').value.trim(),shift=content.querySelector('#yearNewClassShift').value;if(!name){toast('Informe o nome da nova turma.');return}if(destinations.some(d=>normalize(d.name)===normalize(name)&&d.shift===shift)){toast('Essa turma já foi adicionada.');return}destinations.push({name,shift});renderBuilder()};
    content.querySelectorAll('[data-pool-student]').forEach(box=>box.onchange=()=>{box.checked?selected.add(box.dataset.poolStudent):selected.delete(box.dataset.poolStudent)});
    content.querySelector('#yearResetBuilder').onclick=()=>{assignments.forEach((_,id)=>setAssignment(id,{status:'pending',class_name:'',shift:'Matutino'}));selected.clear();renderBuilder();toast('Distribuição reiniciada. As turmas criadas foram mantidas.')};
    content.querySelectorAll('[data-batch]').forEach(button=>button.onclick=()=>{const status=button.dataset.batch,ids=[...selected];if(!ids.length){toast('Selecione pelo menos um aluno.');return}const terminalInvalid=ids.some(id=>isTerminalClass(students.find(s=>s.id===id)?.className))&&status==='active';if(terminalInvalid){toast('Alunos da 3ª série não podem ser remanejados. Use concluinte, repetente ou transferido.');return}const destinationIndex=Number(content.querySelector('#yearBatchDestination').value),destination=destinations[destinationIndex];if(['active','repeated'].includes(status)&&!destination){toast('Escolha a turma de destino.');return}if(status==='concluded'&&ids.some(id=>!isTerminalClass(students.find(s=>s.id===id)?.className))){toast('Somente alunos da 3ª série podem concluir a etapa.');return}ids.forEach(id=>setAssignment(id,{status,class_name:destination?.name||'',shift:destination?.shift||'Matutino'}));selected.clear();renderBuilder();toast(`${ids.length} aluno${ids.length===1?'':'s'} atualizado${ids.length===1?'':'s'}.`)});
    content.querySelectorAll('[data-remove-destination]').forEach(button=>button.onclick=()=>{const index=Number(button.dataset.removeDestination),removed=destinations[index];if([...assignments.values()].some(x=>['active','repeated'].includes(x.status)&&normalize(x.class_name)===normalize(removed.name)&&x.shift===removed.shift)){toast('Esta turma já possui alunos. Mova-os antes de removê-la.');return}destinations.splice(index,1);renderBuilder()});
    footer.innerHTML=`<b>${students.length-s.pending} de ${students.length} alunos definidos</b>${s.pending?`${s.pending} ainda precisam de uma situação.`:'Plano pronto para revisão.'}`;
  }
  function renderReview(error='') {
    const s=stats(), destinations=new Map();[...assignments.values()].filter(x=>['active','repeated'].includes(x.status)).forEach(x=>{const k=`${x.class_name}|${x.shift}`;destinations.set(k,(destinations.get(k)||0)+1)});
    content.innerHTML=`<div class="year-section-head"><div><h3>Revise antes de preparar ${targetYear}</h3><p>Esta é a última etapa. A alteração só acontece ao clicar em “Confirmar e preparar”.</p></div></div><div class="year-review-grid five"><div class="year-review-stat"><span>REMANEJADOS</span><strong>${s.moved}</strong></div><div class="year-review-stat"><span>REPETENTES</span><strong>${s.repeated}</strong></div><div class="year-review-stat"><span>TRANSFERIDOS</span><strong>${s.transferred}</strong></div><div class="year-review-stat"><span>CONCLUINTES</span><strong>${s.concluded}</strong></div><div class="year-review-stat"><span>NOVAS TURMAS</span><strong>${s.classes}</strong></div></div><div class="year-review-box ${s.pending?'warning':'success'}"><h4>${s.pending?`Ainda faltam ${s.pending} alunos`:'Plano completo e pronto'}</h4><div>${s.pending?'Volte e defina a situação de todos os alunos antes de confirmar.':'Todos os alunos possuem uma situação definida. As turmas atuais serão arquivadas e o histórico permanecerá disponível.'}</div>${!s.pending?`<div class="year-review-list">${[...destinations].map(([key,count])=>{const [name,shift]=key.split('|');return `<div class="year-review-row"><span><b>${esc(name)}</b> · ${esc(shift)}</span><span>${count} aluno${count===1?'':'s'}</span></div>`}).join('')}</div>`:''}${error?`<div class="year-error"><b>Não foi possível concluir.</b><br>${esc(error)}</div>`:''}</div>`;
  }
  function render(){step===1?renderYear():step===2?(mode==='build'?renderBuilder():renderClasses()):renderReview();updateChrome();modal.querySelector('.year-main').scrollTop=0}
  async function applyTransition(){const problem=validate();if(problem){toast(problem);step=2;render();return}if(!confirm(`Confirmar a preparação do ano letivo de ${targetYear}? Todos os alunos ativos da escola serão processados conforme esta revisão.`))return;const schoolId=window.getActiveSchoolId?.();if(!schoolId){toast('Selecione uma escola antes de continuar.');return}busy=true;updateChrome();const {data,error}=await db.rpc('apply_school_year_transition',{target_school_id:schoolId,target_school_year:targetYear,assignments:[...assignments.values()]});if(error){busy=false;renderReview(error.message);updateChrome();return}let photoWarning=false;if(data.removed_photo_paths?.length){const {error:e}=await db.storage.from('student-photos').remove(data.removed_photo_paths);photoWarning=!!e}await load();busy=false;content.innerHTML=`<div class="year-success"><div class="year-success-card"><div class="year-success-icon">✓</div><h3>Ano ${esc(data.school_year)} preparado</h3><p>${esc(data.students_moved)} aluno${data.students_moved===1?'':'s'} processado${data.students_moved===1?'':'s'} com sucesso. As turmas anteriores foram arquivadas e o histórico foi preservado.${photoWarning?' Algumas fotos permaneceram no armazenamento.':''}</p><button class="btn primary" type="button" data-year-finish>Voltar ao Carômetro</button></div></div>`;back.classList.add('hidden');next.classList.add('hidden');footer.innerHTML='<b>Preparação concluída</b>As informações do novo ano já estão carregadas.';content.querySelector('[data-year-finish]').onclick=closePlanner}
  function openPlanner(){if(!canPrepare())return;resetPlan();next.classList.remove('hidden');modal.classList.remove('hidden');document.body.style.overflow='hidden';render()}
  function closePlanner(){if(busy)return;modal.classList.add('hidden');document.body.style.overflow=''}
  function refreshVisibility(){button.classList.toggle('hidden',!canPrepare());if(!canPrepare())closePlanner()}
  button.onclick=openPlanner;back.onclick=()=>{if(step>1&&!busy){step--;render()}};next.onclick=async()=>{if(busy)return;if(step===1){if(!Number.isInteger(targetYear)||targetYear<2020||targetYear>2200){toast('Informe um ano letivo válido.');return}step=2;render();return}if(step===2){const problem=validate();if(problem){toast(problem);return}step=3;render();return}await applyTransition()};modal.querySelector('[data-year-close]').onclick=closePlanner;document.addEventListener('keydown',e=>{if(e.key==='Escape'&&!modal.classList.contains('hidden'))closePlanner()});document.addEventListener('carometro:school-context-ready',refreshVisibility);document.addEventListener('carometro:permission-refresh',refreshVisibility);
});
